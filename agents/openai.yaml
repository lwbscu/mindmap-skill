interface:
  display_name: "MindMap"
  short_description: "Build reliable interactive architecture maps"
  default_prompt: "Use $mindmap-skill and its visual, Word-style local rich-text, media, and routing contracts to create or safely update an evidence-backed interactive architecture map."

policy:
  allow_implicit_invocation: true
