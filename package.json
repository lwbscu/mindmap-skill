{
  "name": "mindmap-skill",
  "version": "0.1.0",
  "description": "Interactive evidence-based architecture map app and agent skill",
  "license": "MIT",
  "type": "module",
  "private": false,
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "check": "node --check app/app.js && node --check app/render-svg.mjs && node --check app/service-worker.js && node --check scripts/build-app.mjs && node --check scripts/render-app.mjs && node --check scripts/serve-app.mjs && node --check scripts/open-app.mjs && node --check scripts/install-desktop.mjs && node --check scripts/uninstall-desktop.mjs && node --check scripts/test-app.mjs",
    "test": "node scripts/test-app.mjs",
    "app:build": "node scripts/build-app.mjs",
    "app:render": "node scripts/render-app.mjs",
    "app:serve": "node scripts/serve-app.mjs",
    "app:open": "node scripts/open-app.mjs",
    "app:install-desktop": "node scripts/install-desktop.mjs",
    "app:uninstall-desktop": "node scripts/uninstall-desktop.mjs",
    "ci": "npm run check && npm test && npm run app:build"
  }
}
